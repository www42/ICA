# Topic 18 Networking 2: VPC Endpoints & VPC peering

## VPC endpoints

* Connectivity ohne den Netzwerk overhead, keine IP Adressen nötig, keine Gateways
* Endpoints managed by AWS (scaling, availability)
